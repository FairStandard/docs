---
title: "Work is handed to other services by API call"
description: "Inherited from the platform-wide ruling: no shared job board. When an import needs downstream work (media analysis, case analysis), intake calls that…"
---

Inherited from the platform-wide ruling: no shared job board. When an
import needs downstream work (media analysis, case analysis), intake calls
that service's job endpoint; how each service queues and retries is its
own business.

